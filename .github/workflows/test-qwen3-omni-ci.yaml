name: Qwen3-Omni CI

on:
  workflow_call:
    inputs:
      omni_ci_home:
        description: PR-scoped CI home from the parent omni-ci workflow.
        type: string
        required: false
        default: ""

permissions:
  contents: read
  checks: read
  pull-requests: read

env:
  OMNI_CI_HOME: >-
    ${{ inputs.omni_ci_home != ''
        && inputs.omni_ci_home
        || format('/data/omni-ci/run-{0}', github.run_id) }}
  PYTORCH_ALLOC_CONF: expandable_segments:True
  # NCCL 2.29.7 (torch 2.13 pin) makes a failed NVLS multicast bind fatal at init
  # (NVIDIA/nccl#2077); the TP=2 stages hit it, and two-GPU TP gains nothing from NVLS.
  NCCL_NVLS_ENABLE: "0"

# Invoked by omni-ci.yaml after TTS CI. Restores omni only.
# DAG:
#   stage-1-thinker -> stage-2-tts -> stage-3-mmmu -> stage-4-mmmu-talker
#     -> stage-5-mmsu -> stage-6-mmsu-talker -> stage-7-videomme
#     -> stage-8-videomme-talker -> stage-9-videoamme
#     -> stage-10-videoamme-talker-tp2

jobs:
  stage-1-thinker:
    name: stage 1 - thinker length integration
    runs-on: [self-hosted, h100]
    timeout-minutes: 20
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run thinker length integration test
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_thinker_length.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: thinker-length

  stage-2-tts:
    name: stage 2 - TTS speed + WER + SIM
    needs: stage-1-thinker
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 25
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Prepare speaker similarity assets
        shell: bash
        run: |
          source omni/bin/activate
          python -m benchmarks.metrics.speaker_similarity_assets --warm-cache
        env:
          HF_ENDPOINT: https://huggingface.co
          SEEDTTS_SIM_CACHE_DIR: /github/home/seedtts-wavlm-sim

      - name: Run TTS CI (speed + WER + SIM)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_tts_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          SGLANG_SEEDTTS50_DIR: /github/home/seedtts-50
          SEEDTTS_SIM_CACHE_DIR: /github/home/seedtts-wavlm-sim
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: TTS (speed + WER + SIM)
          artifact-path-globs: |
            */speed_results.json
            */wer_results.json
            */similarity_results.json
          artifact-upload-name: qwen3-omni-tts-results
          artifact-upload-path: /tmp/**/*_results.json

  stage-3-mmmu:
    name: stage 3 - MMMU accuracy + speed
    needs: stage-2-tts
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 20
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run MMMU CI (accuracy + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_mmmu_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: MMMU (accuracy + speed)
          artifact-path-globs: |
            */mmmu/mmmu_results.json
          artifact-upload-name: qwen3-omni-mmmu-results
          artifact-upload-path: /tmp/**/mmmu_results.json

  stage-4-mmmu-talker:
    name: stage 4 - MMMU Talker
    needs: stage-3-mmmu
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 15
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run MMMU Talker CI (WER + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_mmmu_talker_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: MMMU Talker (WER + speed)
          artifact-path-globs: |
            */mmmu_audio*/mmmu_results.json
          artifact-upload-name: qwen3-omni-mmmu-talker-results
          artifact-upload-path: /tmp/**/mmmu_audio*/mmmu_results.json

  stage-5-mmsu:
    name: stage 5 - MMSU accuracy + speed
    needs: stage-4-mmmu-talker
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 20
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run MMSU CI (accuracy + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_mmsu_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: MMSU (accuracy + speed, summary only)
          artifact-path-globs: |
            */mmsu/mmsu_results.json
          artifact-upload-name: qwen3-omni-mmsu-results
          artifact-upload-path: /tmp/**/mmsu/mmsu_results.json
          summary-only: "true"

  stage-6-mmsu-talker:
    name: stage 6 - MMSU Talker
    needs: stage-5-mmsu
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 15
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run MMSU Talker CI (WER + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_mmsu_talker_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: MMSU Talker (WER + speed)
          artifact-path-globs: |
            */mmsu_audio*/mmsu_results.json
          artifact-upload-name: qwen3-omni-mmsu-talker-results
          artifact-upload-path: /tmp/**/mmsu_audio*/mmsu_results.json

  stage-7-videomme:
    name: stage 7 - Video-MME accuracy + speed
    needs: stage-6-mmsu-talker
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 30
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run Video-MME CI (accuracy + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_videomme_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: Video-MME (accuracy + speed)
          artifact-path-globs: |
            */videomme/videomme_results.json
          artifact-upload-name: qwen3-omni-videomme-results
          artifact-upload-path: /tmp/**/videomme/videomme_results.json

  stage-8-videomme-talker:
    name: stage 8 - Video-MME Talker
    needs: stage-7-videomme
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 30
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run Video-MME Talker CI (WER + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_videomme_talker_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: Video-MME Talker (WER + speed)
          artifact-path-globs: |
            */videomme_audio*/videomme_results.json
          artifact-upload-name: qwen3-omni-videomme-talker-results
          artifact-upload-path: /tmp/**/videomme_audio*/videomme_results.json

  stage-9-videoamme:
    name: stage 9 - Video-AMME accuracy + speed
    needs: stage-8-videomme-talker
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 30
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run Video-AMME CI (accuracy + speed)
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_videoamme_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: Video-AMME (accuracy + speed)
          artifact-path-globs: |
            */videoamme/videoamme_results.json
          artifact-upload-name: qwen3-omni-videoamme-results
          artifact-upload-path: /tmp/**/videoamme/videoamme_results.json

  stage-10-videoamme-talker-tp2:
    name: stage 10 - FP8 Thinker TP=2 (Video-AMME Talker)
    needs: stage-9-videoamme
    if: always() && !cancelled()
    runs-on: [self-hosted, h100]
    timeout-minutes: 35
    container:
      image: hongccc/sglang-omni@sha256:02a85f00438c901c72a2eb2ef738974a807f63af3d13084445604f3344067b19
      # note (Yue Yin): TP=2 relay uses pidfd_getfd to pass fds between stage
      # processes; CAP_SYS_PTRACE is required (else every request 500s).
      options: >-
        --rm
        -e NVIDIA_VISIBLE_DEVICES
        -e OMNI_CI_CPUSET
        --cap-add=SYS_PTRACE
        -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: omni

      - name: Run FP8 Thinker TP=2 CI
        shell: bash
        run: |
          source omni/bin/activate
          export PYTHONPATH=$PWD
          bash .github/scripts/run_flaky_pytest.sh \
            pytest tests/test_model/test_qwen3_omni_videoamme_talker_tp2_ci.py -v -s -x
        env:
          HF_ENDPOINT: https://huggingface.co
          TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: FP8 Thinker TP=2 (Video-AMME Talker accuracy + WER + speed)
          artifact-path-globs: |
            */videoamme_audio*/videoamme_results.json
          artifact-upload-name: qwen3-omni-videoamme-talker-tp2-results
          artifact-upload-path: /tmp/**/videoamme_audio*/videoamme_results.json
