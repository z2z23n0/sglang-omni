# running-eval-suite config — every reference table row across the
# 6 benchmarks/eval/benchmark_*.py files is described here.
#
# `running-eval-suite` reads each row, runs the server + client, then
# either replaces the matching row in the benchmark .py file (same
# hardware tag) or appends a new row at the end of that section's
# table (new hardware). "Local v1 Pipeline Result" sections are
# always skipped — those are contributors' personal experiments, not
# refresh targets.
#
# Schema (per row):
#   id                          handle (informational; row selection is by --benchmarks short name)
#   file                        path (relative to repo root) of the benchmark .py
#   hf_model_id                 (optional) overrides the config-level default; needed for s2-pro rows
#   locate.section_substring    substring of the section header above the table (e.g. "Accuracy (accuracy)")
#   locate.config_substring     substring of the row's Config cell (e.g. "modalities=text ")
#   locate.source_workload      workload tag WITHOUT the [hardware,] prefix; e.g. "router, 2-worker, full-set, c=8".
#                               At runtime the skill prepends the detected hw tag → "[H200, router, 2-worker, full-set, c=8]"
#   server_profile              optional server profile override. If omitted,
#                               default_server_profile or server_profile_by_hf_model_id is used.
#   server                      legacy direct server launch template fallback
#   server_extra_by_hardware    optional hardware tag -> extra server args appended to server or profiled worker args
#   server_gpus                 legacy direct server GPU count fallback
#   server_health               optional health URL ({port} substituted), defaults to /health
#   server_boot_timeout_s       optional seconds to wait for /health 2xx; profiles own this by default
#   client                      client launch template; {port} {output_dir} {model} {asr_gpu} substituted.
#                               {asr_gpu} = "cuda:N" of a GPU not given to the server
#                               (or "cpu" if no free GPUs left); use in --asr-device / --device flags.
#   result_json                 path under output_dir where the client writes its JSON
#   cells                       column header → { path, format } OR { paths: { alias: ... }, format }

name: qwen3-omni
description: "Reference benchmarks for Qwen3-Omni 30B-A3B-Instruct + S2-Pro TTS"

# Default model ID; per-row hf_model_id overrides this (s2-pro rows do).
hf_model_id: "Qwen/Qwen3-Omni-30B-A3B-Instruct"

hf_datasets:
  - "ddwang2000/MMSU"
  - "MMMU/MMMU"
  - "zhaochenyang20/Video_MME"
  - "zhaochenyang20/Video_AMME_ci"
  - "zhaochenyang20/Video_MME_ci"
  - "zhaochenyang20/seed-tts-eval-arrow"

# Auxiliary scoring resources — orthogonal to the main model+dataset; without
# them rows usually run-to-completion but skip every sample at the ASR/WER
# step (silent quality fail), or fail-fast at meta-file lookup. precheck
# verifies each is in place and prints the exact install command otherwise.
auxiliary_models:
  - kind: hf
    id: "openai/whisper-large-v3"
    install_cmd: "HF_ENDPOINT=https://hf-mirror.com huggingface-cli download openai/whisper-large-v3"
    used_for: "EN WER ASR (mmsu+audio, mmmu+audio, seedtts EN, videoamme talker)"
  - kind: modelscope
    id: "iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch"
    install_cmd: "<venv-python> -c 'from funasr import AutoModel; AutoModel(model=\"paraformer-zh\")'"
    used_for: "ZH WER ASR (auto-downloaded by funasr's AutoModel('paraformer-zh'))"

python_packages:
  - name: "funasr"
    install_cmd: "uv pip install funasr"
    used_for: "ZH ASR runtime (AutoModel('paraformer-zh'))"
  - name: "zhconv"
    install_cmd: "uv pip install zhconv"
    used_for: "ZH transcribe (T↔S conversion); MISSING SILENTLY SKIPS ALL ZH SAMPLES"
  - name: "zhon"
    install_cmd: "uv pip install zhon"
    used_for: "ZH punctuation set used by normalize_text(text, 'zh')"

default_venv_python:
  - "/sgl-workspace/sglang-omni/.venv/bin/python"
  - "/sgl-workspace/sglang-omni/omni/bin/python"
  - "/github/home/calibration/omni/bin/python"
  - "/github/home/omni/bin/python"
  - "/data/sglang-omni/omni/bin/python"

# Matches .github/actions/omni-setup (calibration slice = /github/home/calibration).
omni_ci_home: "/github/home/calibration"
auto_env:
  HOME: "/github/home"
  OMNI_CI_HOME: "/github/home/calibration"
  HF_HOME: "/github/home/.cache/huggingface"
  MODELSCOPE_CACHE: "/github/home/.cache/modelscope"
  XDG_CACHE_HOME: "/github/home/calibration/.cache"
  HF_ENDPOINT: "https://hf-mirror.com"
  HF_HUB_DISABLE_XET: "1"
  UV_INDEX_URL: "https://pypi.tuna.tsinghua.edu.cn/simple"
  UV_CACHE_DIR: "/github/home/.cache/uv"
  TORCHINDUCTOR_CACHE_DIR: "/github/home/calibration/.torchinductor"
  FLASHINFER_DISABLE_VERSION_CHECK: "1"
  SEEDTTS_SIM_CACHE_DIR: "/github/home/seedtts-wavlm-sim"

default_server_profile: qwen3_omni_colocated_router

server_profile_by_hf_model_id:
  fishaudio/s2-pro: s2pro_router

server_profiles:
  qwen3_omni_colocated_router:
    server_gpus: 2
    model_name: qwen3-omni
    num_workers: 2
    num_gpus_per_worker: 1
    worker_extra_args: "--config {repo_root}/examples/configs/qwen3_omni_colocated.yaml --colocate"
    wait_timeout: 600
    server_boot_timeout_s: 660
    router_policy: least_request
  qwen3_omni_video_router:
    server_gpus: 2
    model_name: qwen3-omni
    num_workers: 2
    num_gpus_per_worker: 1
    worker_extra_args: "--config {repo_root}/examples/configs/qwen3_omni_colocated.yaml --colocate --stages.0.factory-args.thinker-max-seq-len 32768 --stages.thinker.factory-args.thinker-max-seq-len 32768"
    wait_timeout: 600
    server_boot_timeout_s: 660
    router_policy: least_request
  qwen3_omni_mmsu_router:
    server_gpus: 2
    model_name: qwen3-omni
    num_workers: 2
    num_gpus_per_worker: 1
    worker_extra_args: "--config {repo_root}/examples/configs/qwen3_omni_mmsu.yaml --text-only"
    wait_timeout: 600
    server_boot_timeout_s: 660
    router_policy: least_request
  s2pro_router:
    server_gpus: 2
    model_name: fishaudio/s2-pro
    num_workers: 2
    num_gpus_per_worker: 1
    worker_extra_args: "--config {repo_root}/examples/configs/s2pro_tts.yaml"
    wait_timeout: 180
    server_boot_timeout_s: 240
    router_policy: least_request

rows:
  # ===================== mmsu (Qwen3-Omni text-only) =====================
  - id: mmsu-text-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    server_profile: qwen3_omni_mmsu_router
    locate:
      section_substring: "Accuracy (accuracy)"
      config_substring: "modalities=text "
      source_workload: "router, 2-worker, full-set, c=8"
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      overall_accuracy:    { path: "summary.overall_accuracy", format: "{v:.2%}" }
      parseable_samples:   { paths: { v: "summary.parseable_samples", t: "summary.total_samples" }, format: "{v}/{t}" }
      unparseable_samples: { path: "summary.unparseable_samples", format: "{v}" }

  - id: mmsu-text-audio-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    server_profile: qwen3_omni_mmsu_router
    locate:
      section_substring: "Accuracy (accuracy)"
      config_substring: "modalities=text+audio"
      source_workload: "router, 2-worker, full-set, c=8"
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text+audio --max-concurrency 8 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "mmsu_results.json"
    cells:
      overall_accuracy:    { path: "summary.overall_accuracy", format: "{v:.2%}" }
      parseable_samples:   { paths: { v: "summary.parseable_samples", t: "summary.total_samples" }, format: "{v}/{t}" }
      unparseable_samples: { path: "summary.unparseable_samples", format: "{v}" }

  - id: mmsu-text-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    server_profile: qwen3_omni_mmsu_router
    locate:
      section_substring: "Speed (speed)"
      config_substring: "modalities=text "
      source_workload: "router, 2-worker, full-set, c=8"
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      latency_mean_s: { path: "speed_metrics.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed_metrics.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed_metrics.throughput_qps", format: "{v:.2f}" }
      output_tok_per_req_s:  { path: "speed_metrics.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: mmsu-text-audio-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    server_profile: qwen3_omni_mmsu_router
    locate:
      section_substring: "Speed (speed)"
      config_substring: "modalities=text+audio"
      source_workload: "router, 2-worker, full-set, c=8"
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text+audio --max-concurrency 8 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "mmsu_results.json"
    cells:
      latency_mean_s: { path: "speed_metrics.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed_metrics.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed_metrics.throughput_qps", format: "{v:.2f}" }
      output_tok_per_req_s:  { path: "speed_metrics.output_tok_per_req_s",  format: "{v:.1f}" }

  # ===================== mmmu (Qwen3-Omni text-only c=8 + full-pipeline c=1) =====================
  - id: mmmu-no-audio-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Accuracy (summary)"
      config_substring: "enable_audio=False"
      source_workload: "router, 2-worker, full-set, c=8, max_tokens=2048"
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 2048 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: mmmu-audio-c1-accuracy
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Accuracy (summary)"
      config_substring: "enable_audio=True"
      source_workload: "router, 2-worker, 50-sample subset, c=1, max_tokens=2048"
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 1 --max-tokens 2048 --enable-audio --max-samples 50 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "mmmu_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: mmmu-no-audio-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "enable_audio=False"
      source_workload: "router, 2-worker, full-set, c=8, max_tokens=2048"
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 2048 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "speed.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: mmmu-audio-c1-speed
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "enable_audio=True"
      source_workload: "router, 2-worker, **50-sample subset**, c=1, max_tokens=2048"
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 1 --max-tokens 2048 --enable-audio --max-samples 50 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "mmmu_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "speed.output_tok_per_req_s",  format: "{v:.1f}" }

  # ===================== seedtts (Qwen3-Omni full-pipeline talker, c=16) =====================
  # NOTE: the EN VC=T accuracy row's reference Source carries `n=3 mean`. If
  # you run --rounds 1 here the cell values reflect a single round; the
  # locator still matches but the resulting reference is no longer a 3-round
  # mean. Run --rounds 3 and manually average if you need a true n=3 mean
  # (multi-round aggregation isn't built in yet).
  - id: seedtts-en-vc-t-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, voice_clone=T"
      source_workload: "router, 2-worker, full-set, c=16, n=3 mean"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-en-vc-f-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, voice_clone=F"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-zh-vc-t-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, voice_clone=T"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-zh-vc-f-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, voice_clone=F"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-en-vc-t-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, voice_clone=T"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: seedtts-en-vc-f-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, voice_clone=F"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: seedtts-zh-vc-t-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, voice_clone=T"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: seedtts-zh-vc-f-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, voice_clone=F"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  # ===================== videomme (Qwen3-Omni text-only c=4, 2520 samples) =====================
  - id: videomme-thinker-only-c4-accuracy
    file: "benchmarks/eval/benchmark_omni_videomme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Accuracy (full set)"
      config_substring: "thinker-only, full-set, c=4"
      source_workload: "router, 2-worker, c=4, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videomme --model qwen3-omni --port {port} --max-concurrency 4 --max-tokens 256 --video-fps 2 --video-max-frames 128 --video-max-pixels 401408 --output-dir {output_dir}"
    result_json: "videomme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videomme-thinker-only-c4-speed
    file: "benchmarks/eval/benchmark_omni_videomme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Speed (full set)"
      config_substring: "thinker-only, full-set, c=4"
      source_workload: "router, 2-worker, c=4, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videomme --model qwen3-omni --port {port} --max-concurrency 4 --max-tokens 256 --video-fps 2 --video-max-frames 128 --video-max-pixels 401408 --output-dir {output_dir}"
    result_json: "videomme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      output_tok_per_req_s:       { path: "speed.output_tok_per_req_s",    format: "{v:.1f}" }
      output_tokens_mean:     { path: "speed.output_tokens_mean",  format: "{v}" }
      output_tokens_total:    { path: "speed.output_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  # ===================== videoamme (Qwen3-Omni; ci-50 thinker-only + ci-50 thinker-talker) =====================
  - id: videoamme-thinker-only-ci50-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Accuracy"
      config_substring: "thinker-only, ci-50, c=8"
      source_workload: "router, 2-worker, c=8, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videoamme-thinker-talker-ci50-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Accuracy"
      config_substring: "thinker-talker, ci-50, c=8"
      source_workload: "router, 2-worker, c=8, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "videoamme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videoamme-thinker-only-ci50-c8-speed
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Speed"
      config_substring: "thinker-only, ci-50, c=8"
      source_workload: "router, 2-worker, c=8, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      output_tok_per_req_s:       { path: "speed.output_tok_per_req_s",    format: "{v:.1f}" }
      output_tokens_mean:     { path: "speed.output_tokens_mean",  format: "{v}" }
      output_tokens_total:    { path: "speed.output_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  - id: videoamme-thinker-talker-ci50-c8-speed
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Speed"
      config_substring: "thinker-talker, ci-50, c=8"
      source_workload: "router, 2-worker, c=8, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "videoamme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      output_tok_per_req_s:       { path: "speed.output_tok_per_req_s",    format: "{v:.1f}" }
      output_tokens_mean:     { path: "speed.output_tokens_mean",  format: "{v}" }
      output_tokens_total:    { path: "speed.output_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  - id: videoamme-thinker-talker-ci50-c8-wer
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    server_profile: qwen3_omni_video_router
    locate:
      section_substring: "Talker WER"
      config_substring: "thinker-talker, ci-50, c=8"
      source_workload: "router, 2-worker, c=8, max_tokens=256"
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir} --asr-device {asr_gpu}"
    result_json: "videoamme_results.json"
    cells:
      evaluated:             { paths: { v: "wer.summary.evaluated", t: "wer.summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "wer.summary.skipped", format: "{v}" }
      wer_corpus:            { path: "wer.summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "wer.summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_p95:    { path: "wer.summary.wer_per_sample_p95",    format: "{v:.2%}" }
      wer_per_sample_max:    { path: "wer.summary.wer_per_sample_max",    format: "{v:.2%}" }
      n_above_50_pct_wer:    { path: "wer.summary.n_above_50_pct_wer",    format: "{v}" }
      audio_duration_mean_s: { path: "wer.summary.audio_duration_mean_s", format: "{v:.3f}" }

  # ===================== s2-pro tts (separate model, fishaudio/s2-pro) =====================
  - id: s2pro-tts-en-stream-false-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, stream=False"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-en-stream-true-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, stream=True"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-zh-stream-false-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, stream=False"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-zh-stream-true-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, stream=True"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --device {asr_gpu}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total_samples" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-en-stream-false-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, stream=False"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: s2pro-tts-en-stream-true-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, stream=True"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang en --output-dir {output_dir} --device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: s2pro-tts-zh-stream-false-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, stream=False"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }

  - id: s2pro-tts-zh-stream-true-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, stream=True"
      source_workload: "router, 2-worker, full-set, c=16"
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta zhaochenyang20/seed-tts-eval-arrow --max-concurrency 16 --lang zh --output-dir {output_dir} --device {asr_gpu}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "summary.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "summary.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "summary.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "summary.throughput_qps", format: "{v:.3f}" }
      output_tok_per_req_s:  { path: "summary.output_tok_per_req_s",  format: "{v:.1f}" }
